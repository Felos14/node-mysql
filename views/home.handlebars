<main>
    <h2>Home</h2>

    {{#each books}} 
    <div class="book">
        <h2>Título: {{this.name}}</h2>
        <p>Quantidade de páginas: {{this.pageqty}}</p>
        <a href="/book/{{this.id}}">Ver mais</a>
    </div>
    {{/each}}
</main>